# progress — Что работает, что осталось

_Обновлено: 2026-07-14_

## Статус

**Этап 1 (порт на новую архитектуру): завершён** (влит в master).

**Фича «Материал поверхности и разрушаемость»: завершена** (влита в master).

**Воксельная кора (этап 1 реальной разрушаемости): завершена** (влита в master 2026-07-08).

**Обломки-глыбы (этап 2 реальной разрушаемости): завершены** (влиты в master 2026-07-14).

**Трещины и целостность (этап 3): завершены** (влиты в master 2026-07-14).

**Раскол планеты (этап 4): реализован** (ветка `feat/planet-shatter`). **Дорожная карта
«реальной разрушаемости» (спека 2026-07-06 §3) полностью закрыта.** Дальше — бэклог (ниже).

## Раскол планеты (2026-07-14)

- ✅ **ShatterState** (`render/shatterState.ts`, чистый TS): intact→agony→shattered;
  boost 0→1 линейно за SHATTER_AGONY_T=4.5с; событие 'shatter' ровно один раз; reset;
  повторный trigger — no-op. 5 юнит-тестов.
- ✅ **Агония**: глобальный буст трещин — 4-й аргумент `crackEmissiveNode(crackR, p, uTime,
  boost)`, `effCrack = clamp(R + boost)` → жилы разгораются по всей планете; тряска растёт с
  boost; `playShatter(0.7)`.
- ✅ **Распад**: `GlobeView.setPlanetVisible(false)` (глобус+атмосфера), океан
  `oceanShell.mesh.visible`, `CrustView.setVisible(false)`, `TileLayers.setVisible(false)`
  (новый метод, из main.ts); `MagmaCore.setBoost` (ядро к бело-жёлтому);
  `DebrisView.emitShatter(seed, now)` — 140 крупных вечных осколков (орбитальный сегмент,
  r 1.05–1.9, размер 0.05–0.18, медленное кувыркание); `playShatter(1.6)` + толчок тряски.
- ✅ **Глушение ударов** в shattered: Scene пропускает missileLaunched/explosionStarted
  (three-рейкаст не учитывает visible — глушим на приёмнике, ввод не трогаем).
- ✅ **HUD**: баннер `#shatter` «☠ ПЛАНЕТА РАСКОЛОТА» (стиль в styles.css), кэш-сеттер;
  reset прячет. `playShatter` — новый синтез в effects/sound.ts (10с, ФНЧ 300→18 Гц).
- ✅ **Киношный разрыв на куски** (ревизия по фидбэку юзера, спека §5): оболочка сферы
  (внешний r=1, внутренний SHATTER_SHELL_INNER=0.952) бьётся Вороным на ~14 ЗАМКНУТЫХ
  кусков (`render/shatterShards.ts`, чистый TS: икосфера 1280 треугольников, корка+изнанка+
  стенки; тесты: замкнутость по чётности рёбер, сохранение сферы, детерминизм).
  `ShatterShardsView`: в момент 'shatter' куски бесшовно подменяют глобус (вместе — та же
  сфера) и в замедленном темпе (разгон smoothstep за SHATTER_PLATE_RAMP_T=6с от нуля)
  разлетаются в стороны с медленным кувырканием; корка — биом по локальной позиции
  (кусок несёт свой участок карты), срезы/изнанка — градиент порода→магма с эмиссией.
  Движение — CPU-трансформы ~14 мешей (без аллокаций). Reset dispose'ит куски.
- ✅ **От планеты не остаётся ничего** (ревизия №2 по фидбэку, спека §6): через
  SHATTER_CORE_LINGER_T=7с после раскола — распад ядра: фазы `collapse` (coreProgress 0→1
  за 2.2с: первая половина — слепящая вспышка магмы с раздуванием, вторая — схлопывание в
  ноль) → `gone`. Под вспышку кольцо мусора очищается и заменяется `emitEscape` (~200
  обломков на «орбиты» r=6–14 — за секунды уходят из вида). Итог: пустота, звёзды, вдаль
  улетают куски-плиты, баннер. Reset возвращает магму (scale/visible) и всё остальное.
- ✅ Проверки: 116 юнит-тестов, tsc, vite build, eslint+prettier — чисто. Визуальная
  приёмка — за юзером (добить до 0% или временно снизить CRUST_DOOM_VOXELS).

## Трещины и целостность (2026-07-14)

- ✅ **Crust**: `deepestLayer` в CarveResult (−1 если ничего); `integrity()` = 1 −
  removedVoxels/CRUST_DOOM_VOXELS (кламп [0,1], бюджет 20k вокселей ≈ 34×100Мт);
  `crackStrengthForDepth` (слой <5 → 0; 5→⅓; 7→1) — всё под юнит-тестами.
- ✅ **R-канал DamageField переназначен** на очаги трещин: uCrack × спад от края ямы до
  2.4 радиусов штампа (был «глубина чаши», никем не читался). G/B/A — без изменений;
  неглубокий удар (uCrack=0) канал не трогает (ping-pong max сохраняет старые очаги).
- ✅ **`render/effects/cracks.ts`** — общий эмиссивный узел: ridged fbm3 по направлению
  фрагмента (рисунок не зависит от разрешения поля — мыла на зуме нет), пороги
  CRACK_EDGE0/1, пульс sin(uTime·1.7), цвет магмы CRACK_COLOR; `setEmissiveNode` —
  типобезопасный обход дыры @types/three (emissiveNode есть у всех NodeMaterial в рантайме).
- ✅ **Подключение в оба материала**: GlobeView (поверхность) и CrustView (крышки/склоны
  чанков) — рисунок совпадает на границе дискарда по построению; setTime толкает Scene.update.
- ✅ **Scene**: carve теперь ДО damageField.splat (глубина пробития → сила очага);
  `crustIntegrity` — геттер для HUD.
- ✅ **HUD**: «Целостность коры: N%», DOM только при смене процента, жёлтый <70% /
  красный <35%; опрос из кадрового колбэка main.ts. reset возвращает 100% штатными путями.
- ✅ Проверки: 104 юнит-теста, tsc, vite build, eslint+prettier — чисто. Визуальная
  приёмка — за юзером.

## Обломки-глыбы (2026-07-14)

- ✅ **removedByMat** (`Crust.carve`): разбивка выбитых вокселей по материалам
  (грунт/порода/базальт), инвариант суммы = removed, детерминизм — юнит-тесты.
- ✅ **debrisMath** (`render/debrisMath.ts`, чистый TS): orbitalPos (спираль→вечная орбита,
  CPU-зеркало TSL), landingDir, debrisCount (кламп 8..180 от removed/3), pickMaterial
  (пропорционально разбивке), DebrisSlots (два кольцевых сегмента) — 11 юнит-тестов.
- ✅ **DebrisView** (`render/DebrisView.ts`): InstancedMesh 2560 инстансов (1536 орбитальных +
  1024 баллистических), низкополи-камень (икосаэдр + детерминированный джиттер), кувыркание
  Родригом в TSL, псевдоламберт (без динамического света), непрозрачный материал (гашение
  масштабом). Орбитальное кольцо копится до reset; вытеснение самых старых при переполнении.
- ✅ **Пыхи приземления**: `EjectaView.emitPuff(dir, spawn, seed)` с будущим spawn —
  атрибутная задержка вместо CPU-планировщика; лимит DEBRIS_PUFF_MAX=40 на удар.
- ✅ **Интеграция** (`Scene.startExplosion` суша/лёд): `debrisView.emit(...)` после carve;
  `planetReset` → `debrisView.clear()`. Конфиг — блок DEBRIS_* в `assets/config.ts`.
- ✅ Проверки: 101 юнит-тест зелёный, `tsc --noEmit` чисто, `vite build` ок, eslint+prettier
  чисто. Визуальная приёмка — за юзером (по договорённости харнесс не гоняем).

## Воксельная кора (2026-07-07)

- ✅ **Состояние коры** (`src/crust/`): cube-sphere (6 граней × 256×256×8 вокселей), 384 ленивых чанка
  (32×32×8), Uint8 материал-id; материалы по глубине (грунт/порода/базальт); landmask=вода исключена из
  carve; детерминизм (одна последовательность carve → одно состояние).
- ✅ **Carve API** (`Crust.ts`): `carve(dir, radiusRad, depthVox, seed)` выгрызает эллипсоид по текущей
  поверхности (повторные удары копают глубже); рваные края (seed); счётчик `removedVoxels`
  (с этапа 3 — база `integrity()`); `reset()`.
- ✅ **Мешер Surface Nets** (`surfaceNets.ts`, `chunkGeometry.ts`): гранёные куски без ступенек,
  1-воксельный margin для нормалей/стыков, дедуп квадов между чанками, крышка на r=1 прижата к сфере.
- ✅ **Гибрид-рендер** (`CrustView.ts`, `HoleMask.ts`, `MagmaCore.ts`): нетронутая планета — гладкая
  сфера (GlobeView); повреждённые регионы — воксельные чанки Surface Nets. Маска дырок (equirect,
  wrap/полюсы учтены) → GlobeView discard; воксельный меш рисует яму+нетронутый край. Цвет: биом +
  палитра слоёв + гарь из DamageField. Дно пробития — эмиссивная магма на r≈0.94 (пульсация).
- ✅ **Displacement-кратеры удалены** из `DamageField.ts` (R-чаша, нормали-микрорельеф, vertex
  displacement — демонтированы); поле остаётся ТОЛЬКО для перекраски (G-гарь, B-полынья); морфология
  воксельная.
- ✅ **Интеграция** (`Scene.startExplosion`): `crust.carve + damageField.splat` (G/B) для суши/льда;
  радиус/глубина по мощности (1Мт≈2-4, 10Мт≈7, 100Мт≈15 вокселей; глубина 1Мт=1-2, 10Мт=3, 100Мт=5
  слоёв); `planetReset` → `crust.reset()` + очистка маски.
- ✅ **Юнит-тесты** (vitest): детерминизм carve; углубление при повторном carve; монотонность объёма
  и integrity; oceан не карвится; Surface Nets на эталонном чанке (снапшот вершин/граней, замкнутость);
  пробитие всех слоёв.
- ✅ **Приёмка headless** (`scripts/accept/shots.mjs`, WebGL2): прогрессия 3×100Мт в точку (яма глубже,
  слои темнеют, магма на дне), скол на силуэте (лимб), reset чистит; консоль без ошибок компиляции.
- ⚠️ Отложено: wrap splat у антимеридиана (заявленное приближение); блочные берега из-за маски 512×256
  (не критично для воксельной коры).

## Фича материала/разрушаемости (2026-07-05)

- ✅ **Материал в sim** (авторитетно, без изменения геймплея): `sim/geo.ts::dirToLonLat`;
  `sim/landmask.ts`+`landmask.data.ts` (реальные материки, запечённая маска 512×256 из Blue Marble
  через `scripts/gen-landmask.mjs`, jpeg-js); `sim/material.ts` (`materialAt`/`materialAtDir` →
  `Surface` land/water/ice + `Biome` по широтным поясам+шум); событие `explosionStarted` несёт
  `surface`/`biome`.
- ✅ **Стилизованный биом-глобус**: `render/MaterialGlobe.ts` (процедурная equirect биом-текстура из
  `materialAt`+`biomeRGB`), палитра в `assets/config.ts`; спутниковый слой Esri убран, подписи
  оставлены; HUD больше не обещает снимки.
- ✅ **Разрушаемость (GPU displacement)**: `render/DamageField.ts` — equirect поле урона
  (R=глубина/G=гарь/B=оплавление), splat воронки на детонацию, **ping-pong max** (WebGL2-совместимо,
  без CustomBlending/MaxEquation), `cos(lat)`-коррекция воронки у полюсов; глобус (`render/GlobeView.ts`,
  плотная сетка 384×192) вершинно вдавливается по полю урона и перекрашивается (гарь по G, полынья по B).
  Кратер-декаль из `DecalView` вытеснен полем; осталась только горячая остывающая кайма.
- ✅ **Взрывы по материалу**: маршрутизация в `render/Scene.ts` по `surface`; вода —
  `render/WaterBurstView.ts` (столб/купол/кольцо, в поле НЕ пишет — следа нет); лёд — полынья.
  Общий построитель геометрии купола — `render/effects/geometryUtils.ts` (Explosion+WaterBurst).
- ✅ **Приёмка headless** (`scripts/accept/shots.mjs`, WebGL2 swiftshader): биом-глобус, воронка на
  суше, слияние двух воронок, водный столб без постоянного следа, круглая полынья на льду, reset
  очищает поле — консоль без ошибок компиляции шейдеров. Dev-хуки `__strike/__reset/__lookAt`
  живут в `src/debug/devHooks.ts` за `import.meta.env.DEV` (вырезаются из прод-бандла).
- ⚠️ Отложено/приближения: wrap splat у антимеридиана не сделан (заявленное приближение);
  low-res маска 512×256 даёт блочные берега; тон пыли гриба по биому и «пар» водного взрыва —
  не делали (YAGNI). Материал НЕ влияет на жертвы/геймплей (по решению — только визуал).

## Что портировано (паритет с `reference/earth-nuke.html`, Task 1–10)

- ✅ Core: фиксированный игровой таймстеп (`core/time.ts`, `core/GameLoop.ts`), типизированная
  шина событий (`core/EventBus.ts`), сидированный RNG.
- ✅ Sim/ECS (чистый слой, без three.js): гео-проекции и тайловая математика (`sim/geo.ts`),
  ECS-мир боеголовок (miniplex, `ecs/world.ts`/`ecs/components.ts`), ~220 городов
  (`sim/cities.ts`), расчёт жертв (`ecs/systems/CasualtySystem.ts`), команды/события
  (`sim/commands.ts`/`sim/events.ts`), `Simulation`/`SimHost` (`sim/Simulation.ts`/`sim/SimHost.ts`).
- ✅ Render: WebGPU-рендерер с откатом на WebGL2 за интерфейсом (`render/Renderer.ts`,
  `render/backend/createThreeRenderer.ts`); 3D-глобус + атмосфера на TSL-узлах (`render/GlobeView.ts`);
  камера (инерция вращения, автоповорот, зум, тряска — `input/CameraRig.ts`) и указатель
  (драг/зум/клик-детонация — `input/PointerController.ts`); спутниковые тайлы Esri + слой
  границ/названий с LOD и LRU-кэшем (`render/TileLayers.ts`); процедурная модель МБР в пуле
  (`render/MissileView.ts`); взрыв — огненный шар/ударная волна в пуле на TSL-узлах
  (`render/ExplosionView.ts`) + GPU-инстансированные частицы гриба (`render/effects/particles.ts`);
  кратеры-декали в пуле с лимитом 512 (переиспользование самого старого) + остывающая
  раскалённая кайма (`render/DecalView.ts`, `render/DecalPool.ts`); синтезированный звук взрыва
  (`render/effects/sound.ts`). **Никакого динамического света** (PointLight эталона на взрыв
  убран — источник лагов; вспышка/кайма — additive-декали/материалы).
- ✅ UI: HUD (счётчики бомб/мегатонн/жертв, лента поражённых городов с задержкой прихода
  волны, кнопки мощности 1/10/100 Мт, «Восстановить планету», «Границы и названия») —
  `ui/Hud.ts` + `ui/styles.css`, единственный потребитель кнопок — `SimHost.post()`.
- ✅ Паритет подтверждён headless-скриншотами (глобус, зум+тайлы+подписи, полёт ракеты,
  взрыв 100 Мт, кратер после остывания, удар по Москве со счётчиком/лентой) и на обычном
  Chrome (WebGPU) + headless swiftshader (WebGL2) — оба без ошибок консоли.
- ✅ Лаги при частых ударах устранены: стресс-тест 15 ударов подряд — среднее время кадра
  ~2 мс (цель <20 мс), без подвисаний. Причина устранена: GPU-инстансинг частиц, пулы на
  ракеты/взрывы/декали (все с лимитом и переиспользованием), отказ от `PointLight`.
- ✅ Постоянное управление только через HUD и мышь — временные отладочные хуки
  (`__strike`/`__boom`/`__perf`/`__setZoom`/`__tileMeshCount`, клавиша `L`) удалены из финального
  кода после приёмки.

## После этапа 1 (бэклог, не сейчас)

Симуляция в Web Worker · сетевой слой (онлайн) · страны/фракции · дипломатия и эскалация ·
радиация/ядерная зима · цели/миссии · день-ночь терминатор · экран итогов · код-сплиттинг
продакшен-бандла (three/webgpu даёт один чанк >500кб — предупреждение vite build, не блокер).
