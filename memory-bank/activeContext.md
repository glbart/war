# activeContext — Текущий фокус

_Обновлено: 2026-07-04_

## Чем занимаемся прямо сейчас

**Этап 1 — перенос демо на новую архитектуру.** Цель: воспроизвести всю функциональность
`reference/earth-nuke.html` на Vite + TypeScript + ECS (miniplex) + WebGPU-рендерер (с откатом
на WebGL2), устранив лаги при частых ударах. **Новых игровых фич не добавляем.**

## Согласованные решения (из брейншторма)

- Сборка: **Vite + npm + TypeScript**. ✅ подтверждено пользователем.
- Архитектура: **ECS**. ✅ подтверждено.
- Рендерер: **WebGPU с откатом на WebGL2**, за интерфейсом. ✅ подтверждено.
- Скоуп этапа 1: **порт с паритетом функций, без новых фич**. ✅ подтверждено.

## Решения подтверждены (2026-07-04)

- **ECS-библиотека — miniplex.** ✅
- **Эффекты на TSL** (узловые материалы three), не сырой GLSL. ✅
- Структура каталогов из `systemPatterns.md` принята без правок. ✅

Спека этапа 1: `docs/superpowers/specs/2026-07-04-architecture-port-design.md`.

## Следующие шаги

1. Зафиксировать спеку этапа 1 в `docs/superpowers/specs/` и получить одобрение пользователя.
2. Перейти к плану реализации (skill `writing-plans`).
3. Скаффолдинг: `git init`, `npm`-проект, Vite, tsconfig strict, ESLint/Prettier, Vitest.
4. Перенос по слоям: core/loop → sim/ecs (гео, города, взрыв, жертвы) → render (глобус, тайлы,
   инстанс-частицы, декали) → input → ui. Проверка паритета скриншотами.

## Важные нюансы

- Общение с пользователем — **на русском** (см. `CLAUDE.md`).
- Демо-файл трогать нельзя до подтверждённого паритета — это эталон.
